[project]
name = "site-dea-farma"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask==3.1.3"
]

# You can leave this here, though Vercel reads vercel.json instead
[tool.vercel]
entrypoint = "my_package.api:app"